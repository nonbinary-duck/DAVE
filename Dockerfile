# ───────────────────────────────────────────────────────────────────────────────
# 1️⃣  Base image – a minimal Miniconda 3 image
# ───────────────────────────────────────────────────────────────────────────────
FROM continuumio/miniconda3

# ───────────────────────────────────────────────────────────────────────────────
# 2️⃣  Install a handful of OS utilities (curl, wget, git, etc.)
# ───────────────────────────────────────────────────────────────────────────────
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        curl wget ca-certificates git vim nano && \
    rm -rf /var/lib/apt/lists/*

# ───────────────────────────────────────────────────────────────────────────────
# 3️⃣  Create a non‑root user (`daveusr`) and initialise conda
# ───────────────────────────────────────────────────────────────────────────────
RUN useradd --create-home --home-dir /daveusr --shell /bin/bash daveusr
USER daveusr
RUN conda init

# ───────────────────────────────────────────────────────────────────────────────
# 4️⃣  Create the conda environment `dave` and install the core libs
# ───────────────────────────────────────────────────────────────────────────────
RUN conda create -n dave python=3.8 -y && \
    conda run -n dave pip install --upgrade pip && \
    conda run -n dave conda install -y \
        pytorch==2.1.2 torchvision==0.16.2 torchaudio==2.1.2 pytorch-cuda=11.8 \
        -c pytorch -c nvidia && \
    conda run -n dave pip install \
        numpy scikit-image scikit-learn tqdm pycocotools transformers \
        gradio==4.44.1          # <‑‑ fixed Gradio version

# ───────────────────────────────────────────────────────────────────────────────
# 5️⃣  Copy all .pth checkpoints into the image
# ───────────────────────────────────────────────────────────────────────────────
COPY *.pth /app/DAVE/weights/

# ───────────────────────────────────────────────────────────────────────────────
# 6️⃣  Set working directory and copy the entire repo
# ───────────────────────────────────────────────────────────────────────────────
WORKDIR /app
COPY . /app

# ───────────────────────────────────────────────────────────────────────────────
# 7️⃣  Default build arguments – can be overridden at build time
# ───────────────────────────────────────────────────────────────────────────────
ARG GRADIO_PORT=7860
ARG MODEL_PATH=/app/DAVE/weights
ARG IMAGE_SIZE=384
ARG CUDA_VISIBLE_DEVICES=0

# ───────────────────────────────────────────────────────────────────────────────
# 8️⃣  Environment variables (set at runtime)
# ───────────────────────────────────────────────────────────────────────────────
ENV GRADIO_PORT=${GRADIO_PORT}
ENV MODEL_PATH=${MODEL_PATH}
ENV IMAGE_SIZE=${IMAGE_SIZE}
ENV CUDA_VISIBLE_DEVICES=${CUDA_VISIBLE_DEVICES}

# ───────────────────────────────────────────────────────────────────────────────
# 9️⃣  Expose the Gradio UI port (even if we’re not launching Gradio now)
# ───────────────────────────────────────────────────────────────────────────────
EXPOSE ${GRADIO_PORT}

# ───────────────────────────────────────────────────────────────────────────────
# 🔟  Final command – run demo_zero_gradio.py with the fixed arguments
#     (plain‑text CMD – Docker will invoke /bin/sh -c automatically)
# ───────────────────────────────────────────────────────────────────────────────
CMD bash -c "source /opt/conda/etc/profile.d/conda.sh && conda activate dave && python demo_zero_gradio.py \
    --prompt_shot \
    --zero_shot \
    --two_passes \
    --skip_train \
    --model_name DAVE_0_shot \
    --model_path $MODEL_PATH \
    --backbone resnet50 \
    --swav_backbone \
    --reduction 8 \
    --num_enc_layers 3 \
    --num_dec_layers 3 \
    --kernel_dim 3 \
    --emb_dim 256 \
    --num_objects 3 \
    --num_workers 8 \
    --use_objectness \
    --use_appearance \
    --batch_size 1 \
    --pre_norm"